import {StrictMode} from 'react';
import {createRoot} from 'react-dom/client';
import App from './App.tsx';
import './index.css';

createRoot(document.getElementById('vls-game-broadcast-2026')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
